/**
 * Computes the minimum and maximum health across DNA strands.
 * @param {string[]} genes - array of gene strings
 * @param {number[]} health - array of health scores corresponding to genes
 * @param {{start: number, end: number, d: string}[]} strands - array of strand queries
 */
function computeDnaHealth(genes, health, strands) {
    // Write your code here
    return null;
}

module.exports = { computeDnaHealth };
